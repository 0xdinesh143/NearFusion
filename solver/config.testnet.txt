# CrossFusion Solver Configuration - Testnet
# ===========================================
# 
# To use this configuration:
# 1. Copy this file to .env: cp config.testnet.txt .env
# 2. Remove the comments and format as environment variables
# 3. Replace placeholder values with your actual keys and addresses

# NEAR Testnet Configuration
NEAR_NETWORK_ID=testnet
NEAR_ACCOUNT_ID=crossfusion-solver.testnet
NEAR_PRIVATE_KEY=your-near-private-key-here
NEAR_RPC_URL=https://rpc.testnet.near.org
NEAR_ESCROW_FACTORY=crossfusion-factory.testnet

# Base Sepolia Testnet Configuration
BASE_SEPOLIA_RPC_URL=https://sepolia.base.org
BASE_SEPOLIA_ESCROW_FACTORY=0x0000000000000000000000000000000000000000

# EVM Private Key (same for all EVM chains)
EVM_PRIVATE_KEY=your-evm-private-key-here

# Price Feed Configuration
COINGECKO_API_KEY=your-coingecko-api-key-here

# TEE Configuration (Phala Network)
TEE_ATTESTATION_URL=https://attestation.phala.network
PHALA_CLOUD_API_KEY=your-phala-api-key-here

# Server Configuration
SOLVER_PORT=3000
CORS_ORIGINS=http://localhost:3000,http://localhost:3001

# Logging
LOG_LEVEL=debug

# Development Configuration
NODE_ENV=development

# Solver Performance Settings
MIN_PROFIT_THRESHOLD=0.01
MAX_GAS_PRICE_GWEI=50
HEALTH_CHECK_INTERVAL=30000

# ===========================================
# DEPLOYMENT INSTRUCTIONS:
# 
# 1. Replace placeholder values with actual:
#    - NEAR_ACCOUNT_ID: Your solver account on NEAR testnet
#    - NEAR_PRIVATE_KEY: Your NEAR account private key
#    - NEAR_ESCROW_FACTORY: Deployed factory contract address
#    - BASE_SEPOLIA_ESCROW_FACTORY: Deployed factory on Base Sepolia
#    - EVM_PRIVATE_KEY: Your EVM wallet private key
#    - API keys for price feeds and TEE services
#
# 2. Contract Deployment Required:
#    - Deploy EVM contracts to Base Sepolia testnet
#    - Deploy NEAR contracts to testnet
#    - Update the contract addresses above
#
# 3. Setup Commands:
#    cd solver
#    cp config.testnet.txt .env
#    # Edit .env with your actual values
#    npm install
#    npm run build
#    npm start
#
# ===========================================